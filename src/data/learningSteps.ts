import type { LearningStep } from "./topicContent";

// Learning steps for each topic, ordered from beginner → intermediate → hard
export const topicLearningSteps: Record<string, LearningStep[]> = {
  "intro-to-python": [
    { title: "What is Python?", difficulty: "beginner", explanation: "Python is a high-level programming language used for web development, AI, data science, and more. It's the language used in OCR and AQA GCSE Computer Science exams. Python is popular because its syntax is clean and readable — close to plain English.", examples: [{ code: `print("Hello, World!")`, description: "Every programmer's first program — displaying text on screen." }], hints: ["print() is a built-in function that displays output", "Text must be inside quotes — single or double"], interactiveTask: { instruction: "Write a program that prints your name and your favourite subject.", starterCode: `# Print your name\n\n# Print your favourite subject\n` } },
    { title: "Comments and structure", difficulty: "beginner", explanation: "Comments start with # and are ignored by Python. They help explain your code to other programmers (and your future self). Good commenting is required by OCR mark schemes. Python runs code line by line from top to bottom — this is called sequence.", examples: [{ code: `# This is a comment — Python ignores it\nname = "Alice"  # Variable to store the name\nprint(name)     # Display the name`, description: "Comments explain what the code does without affecting the output." }], hints: ["Use # at the start of a line for a comment", "Comments are essential for exam marks — they show understanding"], interactiveTask: { instruction: "Add comments to explain each line of this code.", starterCode: `x = 10\ny = 20\nresult = x + y\nprint(result)` } },
    { title: "Your first program — input and output", difficulty: "beginner", explanation: "Programs interact with users through input (getting data in) and output (displaying results). input() pauses the program and waits for the user to type something. print() displays information on screen. These are the two most fundamental functions in Python.", examples: [{ code: `name = input("What is your name? ")\nprint("Hello, " + name + "!")`, description: "input() always returns a string — even if the user types a number." }, { code: `age = input("How old are you? ")\nprint("You are " + age + " years old")`, description: "Notice age is stored as text, not a number. We'll learn to fix this later." }], hints: ["input() always returns a string data type", "Use + to join (concatenate) strings together", "The prompt message inside input() tells the user what to type"], interactiveTask: { instruction: "Ask the user for their favourite colour and print a message using it.", starterCode: `# Ask for favourite colour\n\n# Print a message including the colour\n` } },
    { title: "Running and debugging", difficulty: "intermediate", explanation: "Debugging is finding and fixing errors in your code. Python shows error messages with the line number and type of error. The three types of error are: Syntax errors (code breaks Python's rules), Runtime errors (code crashes while running), and Logic errors (code runs but gives wrong output). Logic errors are hardest to find because Python doesn't report them.", examples: [{ code: `# Syntax Error — missing closing quote\n# print("Hello)\n\n# Runtime Error — dividing by zero\n# print(10 / 0)\n\n# Logic Error — wrong formula\nradius = 5\n# Should be 3.14 * radius ** 2, not * 2\narea = 3.14 * radius * 2\nprint("Area:", area)  # Wrong answer!`, description: "The logic error gives an answer (31.4) but the correct area is 78.5." }], hints: ["Always read the error message — it tells you the line number", "Syntax errors are caught before the program runs", "Logic errors are the hardest — the code runs but gives wrong results"], interactiveTask: { instruction: "Fix the three bugs in this program.", starterCode: `# Fix all 3 bugs!\nname = input("Enter name: ")\nage = input("Enter age: ")\nprint("Hello " + name)\nprint("Next year you will be " + age + 1)` } },
  ],
  "variables-data-types": [
    { title: "Creating variables", difficulty: "beginner", explanation: "A variable is a named container that stores data. In Python, you create a variable by giving it a name and assigning a value with the = sign. Variable names must start with a letter or underscore, cannot contain spaces, and are case-sensitive (name and Name are different variables).", examples: [{ code: `name = "Alice"\nage = 15\nheight = 1.65\nis_student = True\n\nprint(name)\nprint(age)`, description: "Python figures out the data type automatically — no need to declare it." }], hints: ["Use meaningful names: 'score' not 'x'", "Variable names can't start with numbers or contain spaces", "Use underscores instead of spaces: first_name"], interactiveTask: { instruction: "Create variables for a person's name, age, and whether they like Python (True/False). Print all three.", starterCode: `# Create your variables here\n\n# Print them all\n` } },
    { title: "Data types — String, Integer, Float, Boolean", difficulty: "beginner", explanation: "Python has four main data types for GCSE: String (str) — text in quotes, Integer (int) — whole numbers, Float (float) — decimal numbers, Boolean (bool) — True or False. Use type() to check what type a variable is. This is important for understanding how operations work on different types.", examples: [{ code: `print(type("Hello"))    # <class 'str'>\nprint(type(42))         # <class 'int'>\nprint(type(3.14))       # <class 'float'>\nprint(type(True))       # <class 'bool'>`, description: "type() reveals the data type of any value or variable." }, { code: `# Strings behave differently to numbers!\nprint("3" + "4")   # "34" (concatenation)\nprint(3 + 4)       # 7 (addition)`, description: "Adding strings joins them. Adding numbers sums them. Know the difference!" }], hints: ["Strings are always in quotes — '...' or \"...\"", "Booleans MUST be capitalised: True/False, not true/false", "type() is your debugging friend — use it when unsure"], interactiveTask: { instruction: "Create one variable of each type and use type() to prove their types.", starterCode: `# Create: string, integer, float, boolean\n\n# Print each with type()\n` } },
    { title: "Casting — converting between types", difficulty: "intermediate", explanation: "Casting means converting data from one type to another. int() converts to integer, float() to float, str() to string, bool() to boolean. This is essential because input() always returns a string, so you must cast to int or float before doing maths. Incorrect casting causes ValueError errors.", examples: [{ code: `age_text = "15"\nage_number = int(age_text)\nprint(age_number + 1)  # 16\n\nprice = float("9.99")\nprint(price * 2)  # 19.98`, description: "int() removes decimals (truncates, doesn't round). float() keeps them." }, { code: `# This will crash!\n# number = int("hello")  # ValueError!\n\n# Safe casting with input\nage = int(input("Enter age: "))\nprint("Next year:", age + 1)`, description: "You can only cast strings that look like numbers." }], hints: ["int() truncates decimals: int(3.9) = 3, not 4", "str() can convert anything to text: str(42) = '42'", "Always cast input() before doing maths"], interactiveTask: { instruction: "Ask the user for two decimal numbers, convert them to floats, and print their sum.", starterCode: `# Get two decimal numbers and add them\n` } },
    { title: "OCR exam patterns — type errors and tracing", difficulty: "hard", explanation: "In the OCR exam, you'll be asked to identify data types, trace through code that uses casting, and spot type-related errors. Common questions: 'What is the data type of variable X after line Y?', 'What will this code output?', 'Identify the error in this code.' You must be able to trace through casting operations step by step.", examples: [{ code: `# Exam-style trace question:\nx = "5"\ny = "3"\nz = int(x) + int(y)\nprint(z)           # 8 (integer)\nprint(str(z) + x)  # "85" (string concatenation)`, description: "After int(x) + int(y), z is 8 (integer). str(z) + x = '8' + '5' = '85'" }], hints: ["Trace each line: write down the variable name, value, and TYPE", "When + is used with strings, it concatenates. With numbers, it adds", "int() on a float string like '3.5' will crash — use float() first"], interactiveTask: { instruction: "Predict the output of this code, then run it to check.", starterCode: `a = "10"\nb = "20"\nprint(a + b)\nprint(int(a) + int(b))\nc = str(int(a) + int(b))\nprint(c + a)` } },
  ],
  "arithmetic-operators": [
    { title: "Basic operators: +, -, *, /", difficulty: "beginner", explanation: "Python supports standard arithmetic: + (addition), - (subtraction), * (multiplication), / (division). Division always returns a float, even if the result is a whole number: 10 / 2 gives 5.0, not 5. These work exactly like a calculator.", examples: [{ code: `print(10 + 3)   # 13\nprint(10 - 3)   # 7\nprint(10 * 3)   # 30\nprint(10 / 3)   # 3.3333...`, description: "Division (/) always gives a float. Use // for integer division." }], hints: ["/ always gives a float result", "* is multiplication, not x", "Use brackets for order of operations: (2 + 3) * 4"], interactiveTask: { instruction: "Calculate the total cost of 3 items at £4.50 each, then apply a 20% discount.", starterCode: `price = 4.50\nquantity = 3\n\n# Calculate total and discounted price\n` } },
    { title: "Integer division (//) and modulus (%)", difficulty: "intermediate", explanation: "Integer division (//) divides and rounds DOWN to the nearest whole number. Modulus (%) gives the REMAINDER after division. These two operators are tested heavily in OCR exams. Common uses: extracting digits from numbers, checking if a number is even/odd, converting units (minutes to hours).", examples: [{ code: `print(17 // 5)  # 3 (how many whole times 5 fits into 17)\nprint(17 % 5)   # 2 (the remainder)\n\n# Check even/odd\nnumber = 7\nif number % 2 == 0:\n    print("Even")\nelse:\n    print("Odd")`, description: "// gives the quotient, % gives the remainder. Together they 'split' a division." }, { code: `# Extract digits from 157\nnumber = 157\nhundreds = number // 100        # 1\ntens = (number % 100) // 10     # 5\nunits = number % 10             # 7\nprint(hundreds, tens, units)`, description: "A classic OCR exam question — extracting digits using // and %." }], hints: ["// rounds DOWN: -7 // 2 = -4 (not -3)", "% gives remainder: 10 % 3 = 1 because 10 = 3×3 + 1", "Even check: number % 2 == 0"], interactiveTask: { instruction: "Convert 7384 seconds into hours, minutes, and remaining seconds using only // and %.", starterCode: `total_seconds = 7384\n\n# Convert to hours, minutes, seconds\n` } },
    { title: "Exponentiation and operator precedence", difficulty: "hard", explanation: "Python uses ** for exponentiation (powers). 2 ** 3 = 8 (2 to the power of 3). Operator precedence follows BIDMAS/BODMAS: Brackets → Indices (powers) → Division/Multiplication → Addition/Subtraction. Use brackets to make order explicit — this scores marks in exams.", examples: [{ code: `print(2 ** 3)       # 8\nprint(3 ** 2)       # 9\nprint(2 + 3 * 4)    # 14 (not 20! * before +)\nprint((2 + 3) * 4)  # 20 (brackets first)`, description: "** is exponent. Precedence: brackets > ** > * / // % > + -" }], hints: ["** has higher precedence than * and /", "When in doubt, use brackets — clearer AND avoids bugs", "BIDMAS order applies to Python just like in maths"], interactiveTask: { instruction: "Calculate the area and circumference of a circle with radius 7. Use ** for squaring.", starterCode: `PI = 3.14159\nradius = 7\n\n# Area = π × r²\n# Circumference = 2 × π × r\n` } },
  ],
  "selection-if-else": [
    { title: "Basic if statement", difficulty: "beginner", explanation: "An if statement checks a condition. If the condition is True, the indented code below runs. If False, it's skipped. The condition uses comparison operators: == (equal), != (not equal), > (greater than), < (less than), >= (greater or equal), <= (less or equal). The line MUST end with a colon (:).", examples: [{ code: `age = 18\nif age >= 18:\n    print("You can vote!")\n\ntemperature = 30\nif temperature > 25:\n    print("It's hot today!")`, description: "The indented code only runs when the condition is True." }], hints: ["Don't forget the colon : at the end of the if line", "Use == for comparison, not = (that's assignment)", "Indentation matters — use 4 spaces or Tab"], interactiveTask: { instruction: "Write an if statement that checks if a score is 50 or above, and prints 'Pass' if it is.", starterCode: `score = 72\n\n# Check if pass\n` } },
    { title: "if/else — two-way selection", difficulty: "beginner", explanation: "Add else: to handle the case when the condition is False. This creates a two-way decision: if the condition is True, do one thing; otherwise, do something else. Else doesn't have a condition — it catches everything that if didn't.", examples: [{ code: `number = 7\nif number % 2 == 0:\n    print("Even")\nelse:\n    print("Odd")`, description: "Every number is either even OR odd — perfect for if/else." }], hints: ["else doesn't need a condition — it handles 'everything else'", "else must be at the same indentation level as if", "You can only have ONE else per if statement"], interactiveTask: { instruction: "Check if a number is positive or negative. Print the appropriate message.", starterCode: `number = -5\n\n# Check positive or negative\n` } },
    { title: "if/elif/else — multi-way selection", difficulty: "intermediate", explanation: "Use elif (short for 'else if') when you have more than two possible outcomes. Python checks each condition from top to bottom and runs the FIRST one that's True, then skips the rest. The order matters! Always check from most specific to least specific.", examples: [{ code: `score = 72\n\nif score >= 70:\n    grade = "A"\nelif score >= 60:\n    grade = "B"\nelif score >= 50:\n    grade = "C"\nelif score >= 40:\n    grade = "D"\nelse:\n    grade = "U"\n\nprint(f"Grade: {grade}")`, description: "Check highest grade first. Only ONE branch runs — the first True condition." }], hints: ["Order matters: check highest first, lowest last", "Only ONE branch runs — as soon as a condition is True", "You can have as many elif as you need"], interactiveTask: { instruction: "Create a ticket pricing system: Child (<12) = £5, Teen (12-17) = £7.50, Adult (18-64) = £10, Senior (65+) = £6.", starterCode: `age = int(input("Enter age: "))\n\n# Determine ticket price\n` } },
    { title: "Nested if and complex conditions", difficulty: "hard", explanation: "Nested if statements are if statements inside other if statements — for complex multi-step decisions. You can also combine conditions using 'and' (both must be True), 'or' (at least one True), and 'not' (reverses True/False). In the OCR exam, you'll need to trace through nested selection and identify outputs.", examples: [{ code: `age = 20\nhas_id = True\n\nif age >= 18:\n    if has_id:\n        print("Entry allowed")\n    else:\n        print("Show ID please")\nelse:\n    print("Too young")\n\n# Same logic with 'and':\nif age >= 18 and has_id:\n    print("Entry allowed")`, description: "Nested ifs can usually be simplified with 'and'/'or'. Use whichever is clearer." }], hints: ["and = both conditions must be True", "or = at least one condition must be True", "not reverses: not True = False", "Nested ifs need careful indentation — each level = 4 more spaces"], interactiveTask: { instruction: "Write a program that checks if someone can drive: must be 17+, have a licence, and not be banned.", starterCode: `age = 18\nhas_licence = True\nis_banned = False\n\n# Check all conditions\n` } },
  ],
  "iteration": [
    { title: "Basic for — iterate over a list", difficulty: "beginner", explanation: "A for loop assigns each value from an iterable to a loop variable, one at a time, and runs the indented block for each value. Lists are a common iterable. The loop variable automatically takes the next value on each iteration.", examples: [{ code: `colours = ['red', 'blue', 'green']\nfor colour in colours:\n    print(colour)`, description: "Each item in the list is assigned to 'colour' in turn." }, { code: `for num in [10, 20, 30]:\n    print(num + 5)`, description: "You can use the loop variable in calculations." }], hints: ["The loop variable name can be anything — choose something meaningful", "Don't forget the colon : at the end of the for line"], interactiveTask: { instruction: "Write a for loop to print each name in the list.", starterCode: `# Print each name in the list\nnames = ['Alice', 'Bob', 'Cleo']\n\n# YOUR TASK: Write a for loop to print each name\n# Loop through the names list and display each one\n` } },
    { title: "range() — loop a fixed number of times", difficulty: "beginner", explanation: "Use range() to generate a sequence of integers and iterate a known number of times. range(5) gives 0,1,2,3,4 (5 values). range(start, stop) gives values from start up to but NOT including stop. range(start, stop, step) adds a step value.", examples: [{ code: `for i in range(5):\n    print(i)  # 0, 1, 2, 3, 4`, description: "range(5) generates 5 values starting from 0." }, { code: `for i in range(1, 6):\n    print(i)  # 1, 2, 3, 4, 5`, description: "range(1, 6) starts at 1 and stops BEFORE 6." }, { code: `for i in range(0, 20, 5):\n    print(i)  # 0, 5, 10, 15`, description: "The third argument is the step value." }], hints: ["range(n) gives 0 to n-1 — n values total", "The stop value is EXCLUSIVE — never included", "range(1, 11) gives 1 to 10 — commonly needed!"], interactiveTask: { instruction: "Use range() to print the numbers 1 to 10.", starterCode: `# Print numbers 1 to 10\n` } },
    { title: "Using range() and len() to access list items by index", difficulty: "intermediate", explanation: "Combine range() and len() to loop through a list using index positions. This is useful when you need the index number (e.g., to display position, update specific elements, or compare adjacent items). len(list) returns the number of items.", examples: [{ code: `names = ["Alice", "Bob", "Charlie"]\nfor i in range(len(names)):\n    print(f"Position {i}: {names[i]}")`, description: "i goes 0, 1, 2 — matching the list indexes." }], hints: ["len(list) gives the number of items", "Indexes start at 0, so range(len(list)) gives 0 to len-1", "Use this when you need both the index AND the value"], interactiveTask: { instruction: "Print each item with its position number.", starterCode: `fruits = ["Apple", "Banana", "Cherry", "Date"]\n\n# Print: "1. Apple", "2. Banana", etc.\n` } },
    { title: "While loops — repeat while condition is True", difficulty: "intermediate", explanation: "A while loop runs the indented block as long as the condition is True. Make sure the condition eventually becomes False so the loop finishes. While loops are used when you don't know in advance how many times to loop — e.g., asking until valid input is given.", examples: [{ code: `x = 5\nwhile x > 0:\n    print('Countdown:', x)\n    x = x - 1`, description: "x decreases each time. When x reaches 0, the condition is False and the loop stops." }, { code: `counter = 0\nwhile counter < 4:\n    print('Loop', counter)\n    counter += 1`, description: "+= 1 is shorthand for counter = counter + 1." }], hints: ["Always change the variable in the condition inside the loop", "If the condition never becomes False, you get an infinite loop!", "Use while when you don't know how many iterations you need"], interactiveTask: { instruction: "Write a while loop that counts down from 10 to 1, then prints 'Blast off!'", starterCode: `# Countdown from 10 to 1\n\n# Print "Blast off!"\n` } },
    { title: "Nested loops — loops inside loops", difficulty: "hard", explanation: "A nested loop is a loop inside another loop. The inner loop completes ALL its iterations for EACH iteration of the outer loop. If the outer loop runs 3 times and the inner runs 4 times, the inner code runs 3 × 4 = 12 times total. Used for 2D patterns, grids, and times tables.", examples: [{ code: `for row in range(1, 4):\n    for col in range(1, 4):\n        print(f"({row},{col})", end=" ")\n    print()  # New line`, description: "For each row, the column loop runs completely." }, { code: `# Times table grid\nfor i in range(1, 6):\n    for j in range(1, 6):\n        print(f"{i*j:4}", end="")\n    print()`, description: "A 5×5 multiplication grid using nested loops." }], hints: ["Inner loop runs fully for EACH outer loop iteration", "Total iterations = outer × inner", "Use print(end=' ') to stay on the same line"], interactiveTask: { instruction: "Print a 5x5 grid of stars using nested loops.", starterCode: `# Print a 5x5 grid of * characters\n# Each row should have 5 stars\n` } },
    { title: "Accumulating and filtering with loops", difficulty: "hard", explanation: "Use loops to accumulate values (sum, count, build strings) or filter items from a list. The pattern is: initialise a variable before the loop, update it inside the loop, use it after the loop. This is a fundamental pattern tested in every GCSE exam.", examples: [{ code: `# Sum all numbers\nnumbers = [4, 7, 2, 9, 1]\ntotal = 0\nfor num in numbers:\n    total += num\nprint("Sum:", total)  # 23`, description: "Initialise total=0 before, add each number inside, print after." }, { code: `# Filter: find even numbers\nnumbers = [3, 8, 1, 6, 4, 9, 2]\nevens = []\nfor n in numbers:\n    if n % 2 == 0:\n        evens.append(n)\nprint("Evens:", evens)`, description: "Combine a loop with selection to filter items." }], hints: ["Always initialise the accumulator BEFORE the loop", "total += num is shorthand for total = total + num", "Use .append() to add items to a list inside a loop"], interactiveTask: { instruction: "Find the largest number in a list using a loop (no max() function!).", starterCode: `numbers = [34, 67, 12, 89, 45, 23]\n\n# Find the largest without using max()\n` } },
  ],
  "string-handling": [
    { title: "String basics — length, indexing, slicing", difficulty: "beginner", explanation: "Strings are sequences of characters. Use len() for length, [index] to access a single character (starting from 0), and [start:end] for slicing (extracting a substring). Negative indexes count from the end: [-1] is the last character.", examples: [{ code: `word = "Python"\nprint(len(word))     # 6\nprint(word[0])       # P\nprint(word[-1])      # n\nprint(word[0:3])     # Pyt (slice)`, description: "Indexing starts at 0. Slicing [0:3] gives characters at index 0, 1, 2." }], hints: ["Index 0 is the first character, not index 1", "Slicing [a:b] includes a but excludes b", "Negative indexes: [-1] is last, [-2] is second-to-last"], interactiveTask: { instruction: "Given the word 'Computer', print its length, first letter, last letter, and first 4 characters.", starterCode: `word = "Computer"\n\n# Print length, first, last, first 4 chars\n` } },
    { title: "String methods — upper, lower, strip, replace", difficulty: "beginner", explanation: "Strings have built-in methods: .upper() converts to uppercase, .lower() to lowercase, .strip() removes whitespace from both ends, .replace(old, new) replaces text, .count(char) counts occurrences, .find(char) finds the index of the first occurrence.", examples: [{ code: `name = "  Hello World  "\nprint(name.upper())          # "  HELLO WORLD  "\nprint(name.strip())          # "Hello World"\nprint(name.strip().lower())  # "hello world"\nprint(name.replace("World", "Python"))`, description: "Methods can be chained: .strip().lower() removes spaces then lowercases." }], hints: ["Methods don't change the original — they return a new string", "Chain methods: text.strip().lower()", ".find() returns -1 if not found"], interactiveTask: { instruction: "Clean up user input: remove spaces, convert to lowercase, then check if it equals 'yes'.", starterCode: `user_input = "  YES  "\n\n# Clean and check\n` } },
    { title: "Iterating through strings and character checks", difficulty: "intermediate", explanation: "Strings are iterable — you can loop through each character with a for loop. Use .isalpha() to check if a character is a letter, .isdigit() for a number, .isalnum() for letter or number. These are used for validation in the exam.", examples: [{ code: `word = "Hello123"\nfor char in word:\n    if char.isdigit():\n        print(f"'{char}' is a digit")\n    elif char.isalpha():\n        print(f"'{char}' is a letter")`, description: "Loop through each character and check its type." }], hints: [".isdigit() returns True if the character is 0-9", ".isalpha() returns True for letters only", "Combine with a counter to count specific characters"], interactiveTask: { instruction: "Count the number of vowels in a string.", starterCode: `text = "Hello World"\nvowels = "aeiouAEIOU"\ncount = 0\n\n# Count vowels\n` } },
    { title: "OCR exam — string manipulation problems", difficulty: "hard", explanation: "OCR exam questions frequently ask you to extract initials, reverse strings, validate passwords, or encrypt text. You need to combine indexing, slicing, iteration, and string methods fluently.", examples: [{ code: `# Extract initials\nfull_name = "John Robert Smith"\nparts = full_name.split()\ninitials = ""\nfor part in parts:\n    initials += part[0]\nprint(initials)  # JRS`, description: "Split into words, take first character of each." }], hints: [".split() breaks a string into a list of words", "Access first char of each word with word[0]", "Build result strings using += concatenation"], interactiveTask: { instruction: "Write a function that reverses each word in a sentence but keeps the word order.", starterCode: `sentence = "Hello World Python"\n\n# Reverse each word: "olleH dlroW nohtyP"\n` } },
  ],
  "data-types-casting": [
    { title: "The four GCSE data types", difficulty: "beginner", explanation: "For GCSE you must know: String (str) — text in quotes, Integer (int) — whole numbers without decimals, Float (float) — numbers with decimal points, Boolean (bool) — True or False only. Every value in Python has a type, and the type determines what operations you can perform on it.", examples: [{ code: `name = "Alice"     # str\nage = 15           # int\nheight = 1.65      # float\npassed = True      # bool\n\nprint(type(name))    # <class 'str'>`, description: "type() reveals the data type." }], hints: ["Strings MUST be in quotes", "Booleans are True/False with capital T/F", "Any number with a decimal point is a float"], interactiveTask: { instruction: "Create one variable of each type. Print each with its type.", starterCode: `# Create: str, int, float, bool\n` } },
    { title: "Type casting — int(), float(), str()", difficulty: "intermediate", explanation: "Casting converts between types. int() converts to integer (truncates decimals), float() to decimal, str() to text. This is essential because input() always returns a string. int('hello') crashes with ValueError.", examples: [{ code: `# input always returns a string\nage = input("Age: ")       # "15" (string)\nage = int(input("Age: "))  # 15 (integer)\n\nprint(int(3.9))   # 3 (truncates, doesn't round!)\nprint(float(3))   # 3.0\nprint(str(42))    # "42"`, description: "int() truncates toward zero — it doesn't round." }], hints: ["int() truncates: int(3.9) = 3, int(-3.9) = -3", "Always cast input() before arithmetic", "Use try/except to handle invalid casting"], interactiveTask: { instruction: "Ask for a price (decimal), cast it, calculate 20% VAT, and display the total.", starterCode: `# Get price, calculate VAT, show total\n` } },
  ],
  "input-output-casting": [
    { title: "Getting user input", difficulty: "beginner", explanation: "input() pauses the program and waits for the user to type. It ALWAYS returns a string. To use the input as a number, you must cast it using int() or float(). The text inside input() is the prompt — it tells the user what to type.", examples: [{ code: `name = input("Enter your name: ")\nprint("Hello, " + name + "!")`, description: "input() returns whatever the user types as a string." }], hints: ["input() always returns a string", "Put a space at the end of the prompt for readability", "Cast with int() or float() for numbers"], interactiveTask: { instruction: "Ask for the user's name and birth year, then calculate their age.", starterCode: `# Ask for name and birth year\n# Calculate and display age\n` } },
    { title: "Formatted output with f-strings", difficulty: "intermediate", explanation: "F-strings (formatted string literals) are the modern way to include variables in text. Start the string with f before the quote, then put variables in curly braces {}. You can also include expressions and formatting inside the braces.", examples: [{ code: `name = "Alice"\nage = 15\nscore = 87.5\n\nprint(f"Name: {name}, Age: {age}")\nprint(f"Score: {score:.1f}%")  # 1 decimal place\nprint(f"Next year: {age + 1}")`, description: "f-strings are cleaner than concatenation with +." }], hints: ["f before the quote: f\"...\"", "{variable} inside the string", ":.1f formats to 1 decimal place, :.2f to 2"], interactiveTask: { instruction: "Create a receipt showing 3 items with prices, formatted to 2 decimal places.", starterCode: `item1 = "Pen"\nprice1 = 1.5\n\n# Display a formatted receipt\n` } },
  ],
  "variables-constants": [
    { title: "Variables vs constants", difficulty: "beginner", explanation: "A variable is a value that can change during the program. A constant is a value that should NOT change — it's set once and stays the same. In Python, there's no way to enforce constants, so we use UPPERCASE names as a convention to show they shouldn't be changed: PI = 3.14159, VAT_RATE = 0.20.", examples: [{ code: `# Constants — UPPERCASE names\nPI = 3.14159\nVAT_RATE = 0.20\nMAX_ATTEMPTS = 3\n\n# Variables — lowercase names\nradius = 5\nattempts = 0`, description: "UPPERCASE = constant (don't change). lowercase = variable (can change)." }], hints: ["Python doesn't enforce constants — it's just a naming convention", "Use ALL_CAPS for constants", "Constants make code more readable and easier to maintain"], interactiveTask: { instruction: "Define constants for PI and gravity (9.8), then use them to calculate the area of a circle and the weight of a 70kg object.", starterCode: `# Define constants\n\n# Use them in calculations\n` } },
  ],
  "2d-arrays": [
    { title: "Creating and accessing 2D arrays", difficulty: "beginner", explanation: "A 2D array is a list of lists — like a table with rows and columns. Access elements with two indexes: array[row][column]. Both start at 0, so grid[0][0] is the top-left element.", examples: [{ code: `grid = [\n    [1, 2, 3],\n    [4, 5, 6],\n    [7, 8, 9]\n]\nprint(grid[0][0])  # 1 (first row, first col)\nprint(grid[1][2])  # 6 (second row, third col)`, description: "grid[row][column] — row first, then column." }], hints: ["grid[0] gives the entire first ROW", "grid[0][0] gives the first element of the first row", "Both indexes start at 0"], interactiveTask: { instruction: "Create a 3x3 grid and print the center element.", starterCode: `# Create a 3x3 grid\n\n# Print the center element\n` } },
    { title: "Looping through 2D arrays", difficulty: "intermediate", explanation: "Use nested for loops: the outer loop goes through each row, the inner loop through each element in that row. This lets you process every element in the grid.", examples: [{ code: `grid = [\n    ["A", "B", "C"],\n    ["D", "E", "F"]\n]\nfor row in grid:\n    for item in row:\n        print(item, end=" ")\n    print()`, description: "Outer loop = rows, inner loop = columns." }], hints: ["Outer loop iterates through rows", "Inner loop iterates through elements in each row", "Use end=' ' to print on the same line"], interactiveTask: { instruction: "Create a 4x4 multiplication table grid and display it.", starterCode: `# Create and display a 4x4 times table\n` } },
    { title: "OCR exam — 2D array manipulation", difficulty: "hard", explanation: "OCR exam questions often involve modifying 2D arrays: updating specific cells, searching for values, or processing game boards (noughts and crosses, battleships). You need to combine 2D array access with selection and iteration.", examples: [{ code: `# Search for a value in a 2D array\ngrid = [[1,5,3],[7,2,9],[4,8,6]]\nsearch = 9\nfor r in range(len(grid)):\n    for c in range(len(grid[r])):\n        if grid[r][c] == search:\n            print(f"Found at row {r}, col {c}")`, description: "Use range(len()) to get both position and value." }], hints: ["Use range(len(grid)) when you need the index", "grid[r][c] = new_value to update a cell", "Remember: r is row, c is column"], interactiveTask: { instruction: "Implement a simple seating plan: display the grid, let the user book a seat by entering row and column.", starterCode: `seats = [["O"]*4 for _ in range(3)]\n\n# Display seating plan and book a seat\n` } },
  ],
  "random-numbers": [
    { title: "Generating random integers", difficulty: "beginner", explanation: "Import the random module to generate random numbers. random.randint(a, b) gives a random integer between a and b INCLUSIVE — both ends are included. Always import random at the top of your program.", examples: [{ code: `import random\n\ndice = random.randint(1, 6)\nprint(f"You rolled: {dice}")`, description: "randint(1, 6) gives 1, 2, 3, 4, 5, or 6." }], hints: ["Always import random at the top", "randint includes BOTH endpoints", "Each call gives a different result"], interactiveTask: { instruction: "Simulate rolling two dice and print their total.", starterCode: `import random\n\n# Roll two dice and show the total\n` } },
    { title: "Random choice and practical uses", difficulty: "intermediate", explanation: "random.choice(list) picks one random item from a list. random.random() gives a float between 0.0 and 1.0. These are used for games, quizzes, and simulations.", examples: [{ code: `import random\n\ncolours = ["Red", "Blue", "Green"]\npick = random.choice(colours)\nprint(f"Selected: {pick}")`, description: "choice() picks one random item from any list." }], hints: ["choice() works on any list or string", "random.random() gives 0.0 to 0.999...", "Use random.shuffle(list) to randomise order in-place"], interactiveTask: { instruction: "Create a random password generator that creates an 8-character password from letters and digits.", starterCode: `import random\n\nchars = "abcdefghijklmnopqrstuvwxyz0123456789"\n\n# Generate an 8-character password\n` } },
  ],
  "robust-programming": [
    { title: "Input validation with while loops", difficulty: "beginner", explanation: "Validation ensures user input is sensible before using it. Use a while loop to keep asking until valid input is given. The five types: range check (within bounds), length check (correct length), presence check (not empty), type check (correct data type), format check (correct format).", examples: [{ code: `score = int(input("Score (0-100): "))\nwhile score < 0 or score > 100:\n    print("Must be 0-100!")\n    score = int(input("Score (0-100): "))\nprint(f"Score: {score}")`, description: "Range check: keep asking until 0 ≤ score ≤ 100." }], hints: ["Use while for validation — if only checks once", "The condition in while is the INVALID case", "Always give a clear error message"], interactiveTask: { instruction: "Ask for a password that must be at least 8 characters. Keep asking until valid.", starterCode: `# Get a valid password (8+ characters)\n` } },
    { title: "Try/except for error handling", difficulty: "intermediate", explanation: "try/except catches errors that would crash your program. Put risky code in the try block. If an error occurs, Python runs the except block instead of crashing. ValueError catches invalid type conversions.", examples: [{ code: `while True:\n    try:\n        age = int(input("Age: "))\n        break\n    except ValueError:\n        print("Enter a whole number!")`, description: "If int() fails, the except block runs instead of crashing." }], hints: ["try the risky code, except handles the error", "ValueError = wrong type (e.g., int('hello'))", "Use while True + break for clean validation loops"], interactiveTask: { instruction: "Write a robust number input that handles both type errors and range checking (1-100).", starterCode: `# Get a valid number between 1 and 100\n# Handle both type errors and range errors\n` } },
    { title: "OCR exam — authentication system", difficulty: "hard", explanation: "OCR frequently tests authentication: username/password login with limited attempts. Combine validation, selection, and iteration. After max attempts, lock the account.", examples: [{ code: `correct_user = "admin"\ncorrect_pass = "python123"\nattempts = 0\n\nwhile attempts < 3:\n    user = input("Username: ")\n    pwd = input("Password: ")\n    if user == correct_user and pwd == correct_pass:\n        print("Welcome!")\n        break\n    attempts += 1\n    print(f"{3 - attempts} attempts left")\n\nif attempts == 3:\n    print("Account locked!")`, description: "3 attempts, then lock — a classic OCR exam question." }], hints: ["Track attempts with a counter", "Use break to exit on success", "Check if attempts == max AFTER the loop"], interactiveTask: { instruction: "Create a login system with 3 attempts. Store correct username/password as variables.", starterCode: `# Implement a login system with 3 attempts\n` } },
  ],
  "boolean-logic": [
    { title: "AND, OR, NOT operators", difficulty: "beginner", explanation: "Boolean operators work on True/False values. AND: both must be True. OR: at least one must be True. NOT: flips True↔False. These map directly to Python's 'and', 'or', 'not' keywords.", examples: [{ code: `print(True and True)   # True\nprint(True and False)  # False\nprint(True or False)   # True\nprint(not True)        # False`, description: "AND is strict (both True). OR is lenient (one True). NOT flips." }], hints: ["AND = both must be True", "OR = at least one True", "NOT flips: not True = False"], interactiveTask: { instruction: "Write truth table outputs for A AND B where A and B take all combinations.", starterCode: `# Print AND truth table\nfor a in [True, False]:\n    for b in [True, False]:\n        pass  # Print the result\n` } },
    { title: "Truth tables and precedence", difficulty: "intermediate", explanation: "Truth tables show every possible input combination and output. For 2 inputs: 4 rows (2²). For 3 inputs: 8 rows (2³). Precedence: NOT first, then AND, then OR. Use brackets to override.", examples: [{ code: `# NOT has highest precedence\nprint(not True and False)   # False (not True=False, False and False=False)\nprint(not (True and False)) # True  (True and False=False, not False=True)`, description: "Brackets change the result! Always use them for clarity." }], hints: ["NOT > AND > OR in precedence", "2ⁿ rows for n inputs", "Use brackets to make precedence explicit"], interactiveTask: { instruction: "Generate the truth table for (A AND B) OR (NOT C) — 8 rows.", starterCode: `# Truth table for (A AND B) OR (NOT C)\nprint("A     | B     | C     | Result")\n` } },
  ],
  "insertion-sort": [
    { title: "How insertion sort works", difficulty: "intermediate", explanation: "Insertion sort builds a sorted portion one element at a time. Start from index 1 (element at 0 is 'already sorted'). For each element, compare backwards and shift larger elements right until the correct position is found, then insert.", examples: [{ code: `def insertion_sort(data):\n    for i in range(1, len(data)):\n        key = data[i]\n        j = i - 1\n        while j >= 0 and data[j] > key:\n            data[j + 1] = data[j]\n            j -= 1\n        data[j + 1] = key\n    return data\n\nprint(insertion_sort([5, 3, 1, 4, 2]))`, description: "Like sorting playing cards — insert each card into the right position." }], hints: ["Start from index 1, not 0", "Shift elements right to make room", "Insert when you find the correct position"], interactiveTask: { instruction: "Trace insertion sort on [42, 17, 93, 8] — print the list after each pass.", starterCode: `data = [42, 17, 93, 8]\nprint(f"Start: {data}")\n\n# Implement with tracing\n` } },
    { title: "Comparing sorting algorithms", difficulty: "hard", explanation: "OCR requires you to compare Bubble Sort, Insertion Sort, and Merge Sort. Insertion sort: best O(n), worst O(n²), in-place. Better than bubble sort for nearly-sorted data. Merge sort: always O(n log n) but needs extra memory.", examples: [{ code: `# Count operations for comparison\ndef insertion_ops(data):\n    data = data.copy()\n    ops = 0\n    for i in range(1, len(data)):\n        key = data[i]\n        j = i - 1\n        while j >= 0 and data[j] > key:\n            data[j+1] = data[j]\n            j -= 1\n            ops += 1\n        data[j+1] = key\n    return ops\n\nprint("Nearly sorted:", insertion_ops([1,2,4,3,5]))\nprint("Reversed:", insertion_ops([5,4,3,2,1]))`, description: "Nearly sorted = few operations. Reversed = maximum operations." }], hints: ["Best case O(n) when already sorted", "Worst case O(n²) when reverse sorted", "In-place = no extra list needed (unlike merge sort)"], interactiveTask: { instruction: "Sort a list using insertion sort and count the total number of comparisons and shifts.", starterCode: `data = [64, 34, 25, 12, 22, 11]\ncomparisons = 0\nshifts = 0\n\n# Sort and count\n` } },
  ],
  "sql-basics": [
    { title: "SELECT, FROM, WHERE basics", difficulty: "beginner", explanation: "SQL retrieves data from databases. SELECT columns FROM table WHERE condition. Use * to select all columns. WHERE filters rows. String values must be in quotes. SQL keywords are conventionally UPPERCASE.", examples: [{ code: `# SQL (written on paper in the exam):\n# SELECT * FROM students\n# SELECT name, age FROM students WHERE age > 15\n\n# Python simulation:\nstudents = [{"name":"Alice","age":15},{"name":"Bob","age":17}]\nfor s in students:\n    if s["age"] > 15:\n        print(s["name"], s["age"])`, description: "SQL can't run in Python — learn the syntax for the exam paper." }], hints: ["SELECT * means 'all columns'", "WHERE filters which rows to return", "String values need quotes: WHERE name = 'Alice'"], interactiveTask: { instruction: "Write SQL queries as comments, then simulate them in Python.", starterCode: `# Write SQL: Get all students over 16\n# SQL: \n\n# Simulate in Python:\nstudents = [\n    {"name": "Alice", "age": 15},\n    {"name": "Bob", "age": 17},\n    {"name": "Charlie", "age": 16},\n]\n` } },
    { title: "ORDER BY, LIKE, and operators", difficulty: "intermediate", explanation: "ORDER BY sorts results: ASC (ascending, default) or DESC (descending). LIKE with % wildcard for pattern matching. SQL uses <> for 'not equal' (not != like Python). AND/OR combine conditions.", examples: [{ code: `# ORDER BY:\n# SELECT * FROM students ORDER BY age DESC\n\n# LIKE:\n# SELECT * FROM students WHERE name LIKE 'A%'  -- starts with A\n# SELECT * FROM students WHERE name LIKE '%son' -- ends with son\n\n# Combined:\n# SELECT name FROM students WHERE age > 15 AND grade = 'A'`, description: "% matches any characters. ORDER BY ASC/DESC sorts results." }], hints: ["% is the SQL wildcard (not *)", "<> means 'not equal to' in SQL", "ASC is default (ascending), DESC for descending"], interactiveTask: { instruction: "Write SQL to find products under £10, sorted by price ascending, where name starts with 'S'.", starterCode: `# Write your SQL queries as comments:\n# Query 1: Products under £10\n# \n# Query 2: Sorted by price\n# \n# Query 3: Name starts with 'S'\n# \n` } },
  ],
  "searching-sorting": [
    { 
      title: "The Linear Search", 
      difficulty: "beginner", 
      explanation: "Imagine you're looking for a specific card in a shuffled deck. You'd check them one by one from the top. That's a Linear Search! It works on any list, sorted or not.", 
      examples: [{ code: `def search(items, target):\n    for x in items:\n        if x == target:\n            return True\n    return False`, description: "Checks every item until it finds a match." }], 
      hints: ["Simple but slow for big lists", "Doesn't need the data to be sorted"],
      interactiveTask: { instruction: "Write a loop that prints 'Found' if the number 7 is in the list.", starterCode: `nums = [1, 9, 4, 7, 3]\n# Your code here\n` }
    },
    { 
      title: "The Binary Search", 
      difficulty: "intermediate", 
      explanation: "This is the 'High-Low' game strategy. Open a sorted book to the middle. If your page is higher, throw away the left half. Repeat. It's incredibly fast but ONLY works if the data is sorted.", 
      examples: [{ code: `# Binary search halves the search space every time.\n# 1000 items -> 500 -> 250 -> 125...`, description: "Takes only ~10 steps to search 1,000 items!" }], 
      hints: ["Data MUST be sorted first", "Much faster than linear for large datasets"],
      interactiveTask: { instruction: "Calculate how many steps it takes to find '8' in [1, 3, 5, 7, 8, 9, 11] using Binary Search.", starterCode: `# Mid 1: 7 (Target > 7)\n# Mid 2: 9 (Target < 9)\n# Mid 3: 8 (Found!)\nsteps = 0 # Update this\n` }
    }
  ],
};
